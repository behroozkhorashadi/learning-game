#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/../../frontend"
npx vite build
[[ -d "dist" ]] || { echo "Expected build output at frontend/dist" >&2; exit 1; }
figma make deploy-preview --build-dir dist
